apiVersion: v1
kind: Service
metadata:
  name: {{ include "watchedsky.fullname" . }}
  labels:
    {{- include "watchedsky.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "watchedsky.selectorLabels" . | nindent 4 }}
